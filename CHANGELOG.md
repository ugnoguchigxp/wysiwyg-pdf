# Changelog

## [0.1.2] - 2026-01-02

### Fixed
- Fix lint errors in `src/index.css` and `src/modules/queue/QueueContext.tsx`.
- Fix lint errors in `src/features/report-editor/ReportKonvaEditor.tsx`.
- Fix build and lint issues in `example/backend` (including missing dependencies and configuration).
- Ensure robust type checking and formatting across the project.
